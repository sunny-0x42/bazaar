# QA GRC721 + nft book — 2026-09-20

Owner: bazaar-qa (Dew). No code fix. No Pearl deploy.

Cwd: `C:\Users\Hi\gno-bazaar`. `gno` = `C:\Users\Hi\tools\gno.exe` (`gno version: develop`). `GNOROOT` stdlib: `C:\Users\Hi\tools\gno\gnovm\stdlibs`. Web dump: Vite `http://127.0.0.1:5176` + Puppeteer `web/tmp-qa/qa-puppeteer.mjs` + 8s wait dump.

Packages present (`gno.land/p/bazaar/grc721/{v0,metadata/v0,enumerable/v0,royalty/v0}` untracked). nft book **started** wiring (`nft.gno` import + `attachToken` / `grcMint`) at 12:59–13:00.

---

## Commands

| Command | Cwd | Exit | Result |
| --- | --- | --- | --- |
| `gno test ./gno.land/p/bazaar/grc721/v0 ./gno.land/p/bazaar/grc721/metadata/v0 ./gno.land/p/bazaar/grc721/enumerable/v0 ./gno.land/p/bazaar/grc721/royalty/v0 ./gno.land/r/bazaar/nft` | repo | **1** | **FAIL** 1 build + 5 test errors (quote below). After wiring, **nft also FAIL**. |
| `gno test ./gno.land/p/bazaar/grc721/v0` | repo | **1** | nil deref + `onbloc/diff` setup |
| `gno test ./gno.land/p/bazaar/grc721/metadata/v0` | repo | **1** | `SplitPkgSubPath not declared` |
| `gno test ./gno.land/p/bazaar/grc721/enumerable/v0` | repo | **1** | same |
| `gno test ./gno.land/p/bazaar/grc721/royalty/v0` | repo | **1** | same |
| `gno test ./gno.land/r/bazaar/nft` (after import) | repo | **1** | same (depends on v0) |
| `gno test ./gno.land/r/bazaar/` | repo | 0 | `ok … 0.71s` |
| `gno test ./gno.land/r/bazaar/market/` | repo | 0 | `ok … 0.82s` |
| `gno test ./gno.land/p/bazaar/fee/v1/` | repo | 0 | `ok … 0.63s` |
| `gno test ./gno.land/p/bazaar/dropstd/v1/` | repo | 0 | `ok … 0.64s` |
| `npm test` | `web/` | 0 | 3 files, **105 tests** PASS (`format` 81, `chart` 16, `indexer` 8) |

Quoted batch failure (second run, after nft import):

```
gno: downloading gno.land\p\onbloc\diff
C:\Users\Hi\AppData\Roaming\gno\pkg\mod\gno.land\p\onbloc\diff: get rpc url for pkg path "gno.land\\p\\onbloc\\diff": bad pkg path "gno.land\\p\\onbloc\\diff"
FAIL    .\..\AppData\Roaming\gno\pkg\mod\gno.land\p\onbloc\diff [setup failed]
gno.land\p\bazaar\grc721\v0:gno.land/p/bazaar/grc721/v0/tellers.gno:136:16-37: name SplitPkgSubPath not declared (code=gnoPreprocessError)
FAIL    .\gno.land\p\bazaar\grc721\enumerable\v0 1.12s
gno.land\p\bazaar\grc721\v0:gno.land/p/bazaar/grc721/v0/tellers.gno:136:16-37: name SplitPkgSubPath not declared (code=gnoPreprocessError)
FAIL    .\gno.land\r\bazaar\nft 1.23s
gno.land\p\bazaar\grc721\v0:gno.land/p/bazaar/grc721/v0/tellers.gno:136:16-37: name SplitPkgSubPath not declared (code=gnoPreprocessError)
FAIL    .\gno.land\p\bazaar\grc721\royalty\v0 1.25s
gno.land\p\bazaar\grc721\v0:gno.land/p/bazaar/grc721/v0/tellers.gno:136:16-37: name SplitPkgSubPath not declared (code=gnoPreprocessError)
FAIL    .\gno.land\p\bazaar\grc721\metadata\v0 1.27s
gno.land\p\bazaar\grc721\v0:0: runtime error: invalid memory address or nil pointer dereference (code=gnoUnknownError)
FAIL    .\gno.land\p\bazaar\grc721\v0 1.28s
FAIL
FAIL: 1 build errors, 5 test errors
```

v0-alone extra quote:

```
gno.land\p\bazaar\grc721\v0:0: runtime error: invalid memory address or nil pointer dereference (code=gnoUnknownError)
```

No individual `Test*` names ran. Fail is preprocess / toolchain, not an assertion.

Pearl read-only (`gnomcp` profile `pearl`):

| Query | Result |
| --- | --- |
| `gno_packages` `gno.land/p/bazaar` | empty (local GRC721 **not** on Pearl) |
| `gno_packages` `gno.land/r/bazaar` | empty |
| `gno_packages` `…/g1n4pl5uc4yt5r96m9w6fmdznx3x0jyg8l6arhmt/bazaar` | `nft`, `nftv2`–`nftv5` only. **No parent hub package.** |
| `gno_eval` hub `GetModule("nft")` | `invalid package path` |
| `nftv5.ListCollections()` | bazaar, clay, docks, foxes, lamps, relics, stones |
| `nftv5.ListOpen()` | 21 listed rows (Ember Core / stones …) |
| `nftv5.ListItemsByCollection("stones")` | ids 1–3 listed |

Do **not** addpkg. Local GRC721 cannot compile against this `gno.exe`.

---

## Route checklist (Browse / Sell / Create / Mine / Connect / wrong-network / empty list)

Vite 200. Puppeteer dump `web/tmp-qa/qa-puppeteer-summary.json` + 8s wait. No Adena in Chrome.

| Surface | Route | Verdict | Notes |
| --- | --- | --- | --- |
| Browse | `#/explore` | **FAIL live chain** / PASS catalog | Nav Explore. After 8s: `Realm not on Pearl yet. Showing sample collections.` Ticker = catalog (TYR / Fox / Frogana), not nftv5 Ember Core. See BUG-G3. |
| Sell | `#/sell` | **PASS** empty | h1 `List an item`. Empty: `Connect Adena` / `Connect a wallet to list an item you own.` |
| Create | `#/launch` | **PASS** | h1 `Launch`. Studio 1–5. Load items / Mint 1/1. Fee copy `default 1000 GNOT`. |
| Mine | `#/profile` | **PASS** empty | h1 `Profile`. Empty: `Connect Adena` / `Your collected items, listings, and activity live on your profile.` |
| Connect | header + pages | **PASS** (no Adena) | Header `Install Adena` → `https://adena.app/`. Sell/Profile `Connect Adena` button. |
| wrong-network | source | **PASS source** | No Adena session. `call()` throw **inside** `try` (`App.tsx`). `connectAdena` rejects missing/wrong `chainId`. Banner: `Adena is on {chainId}. Switch to {wantChainId} to buy or sell.` List disabled via `blocked`. |
| empty list | sell / profile / mint | **PASS** copy; Browse **masked** | Sell/Profile empty-state as above. `#/m/stones` empty (BUG-G3). `#/c/stones` `No items`. Explore never empty because catalog fills. |
| `#/m/stones` | mint | **FAIL** | `Drop not on this book` / `This slug is not a live collection drop.` Chain has Signal Stones drop. |
| `#/c/stones` | collection | **FAIL** | `0 of 0 items` / `No items`. Chain `ListItemsByCollection("stones")` has 3 listed. |

Web `npm test` still **does not** cover these surfaces (lib parsers only). Same class as 2026-09-19 BUG-6.

2026-09-19 BUG-1 / BUG-2 (call throw outside try; default `chainId` to Pearl) look **fixed in source**. Not re-tested with Adena.

---

## BUG-G1 — `chain.SplitPkgSubPath` not in local gno → GRC721 + nft tests FAIL

**Severity:** P0 (protocol wiring cannot `gno test`)  
**Where:** `gno.land/p/bazaar/grc721/v0/tellers.gno:136` and `token.gno:45`.

```go
host, _, _ := chain.SplitPkgSubPath(rlm.PkgPath())
origRealm, _, _ := chain.SplitPkgSubPath(pkgPath)
```

`C:\Users\Hi\tools\gno\gnovm\stdlibs\chain\` has no `SplitPkgSubPath` (`rg` empty under `tools/gno`). `gno version: develop`.

nft book now imports this package:

```go
import "gno.land/p/bazaar/grc721/v0"
// attachToken → grc721.NewToken(...)
```

So `gno test ./gno.land/r/bazaar/nft` fails the same way (was **pass** before the import).

**Repro**

1. `cd C:\Users\Hi\gno-bazaar`
2. `C:\Users\Hi\tools\gno.exe test ./gno.land/p/bazaar/grc721/v0`
3. Repeat for `metadata/v0`, `enumerable/v0`, `royalty/v0`, `./gno.land/r/bazaar/nft`

**Expected:** tests compile; `TestNewToken` / `TestMintListBuy` run.  
**Actual:** quote above. `SplitPkgSubPath not declared` (dependents) or nil deref (v0). No `Test*` executed.

**Fix direction (protocol, not QA):** pin GRC721 to this toolchain (strip `#subpath` without `SplitPkgSubPath`) **or** bump `gno.exe` to a stdlib that exports it. Do not deploy Pearl until `gno test` is green.

---

## BUG-G2 — `gno.land/p/onbloc/diff` fetch uses Windows backslash pkg path

**Severity:** P2 (setup noise; GRC721 tests import `uassert` → `onbloc/diff`)  
**Where:** `gno.land/p/nt/uassert/v0/uassert.gno` import `"gno.land/p/onbloc/diff"`; `gno test` on Windows:

```
get rpc url for pkg path "gno.land\\p\\onbloc\\diff": bad pkg path "gno.land\\p\\onbloc\\diff"
FAIL    .\..\AppData\Roaming\gno\pkg\mod\gno.land\p\onbloc\diff [setup failed]
```

nft tests (before GRC721 import) did **not** hit this (`nft_test.gno` uses `testutils`, not `uassert`).

**Repro:** any `gno test` of a package that imports `p/nt/uassert/v0` on this Windows `gno.exe`.

**Expected:** module path stays `gno.land/p/onbloc/diff`.  
**Actual:** backslash path rejected.

---

## BUG-G3 — Browse treats live nftv5 as missing realm; mint/collection empty

**Severity:** P1 (Pearl UI)  
**Where:** `web/src/App.tsx` `refresh`: `ListOpen()` catch → `setChainCollections([])` + **return** (never `ListCollections`). `Explore.tsx` maps `isRealmUnavailable(chainNote)` to copy `Realm not on Pearl yet. Showing sample collections.` (hides the raw error).

**Repro**

1. `cd web; npm run dev` → http://127.0.0.1:5176 (already 200 this run)
2. Fresh Chrome / Puppeteer, wait **8s** on `#/explore`
3. Banner: `Realm not on Pearl yet. Showing sample collections. Open Settings`
4. Ticker: catalog TYR / Fox / Frogana — **not** Ember Core
5. `#/m/stones` → `Drop not on this book`
6. `#/c/stones` → `No items` / `0 of 0 items`

**Expected:** nftv5 `ListOpen` / `ListCollections` / stones mint page (Signal Stones, 0.5 GNOT, 3/6) as in `docs/qa/2026-09-20-launch-ui.md`.  
**Actual:** catalog-only Explore; stones mint/collection empty.

**Chain evidence (same machine, gnomcp `pearl`):** `ListOpen()` 21 rows; `ListItemsByCollection("stones")` 3 items; `ListCollections()` includes `stones|Signal Stones|…|true`. Hub parent `gno.land/r/g1n4pl5uc4yt5r96m9w6fmdznx3x0jyg8l6arhmt/bazaar` is **not** a package (`invalid package path`); UI `resolveNftPath` should fall back to `DEFAULT_NFT` nftv5.

`evalExpr` timeout is 8s (`wallets.ts`). Console also has `/api` 500 (indexer `127.0.0.1:8788` down) — that is **not** `ListOpen`, but 500 + `InternalError` can trip `isRealmUnavailable`.

---

## BUG-G4 — web tests still skip Browse / Sell / Create / Mine / Connect / wrong-network / empty list

**Severity:** P2 (coverage)  
**Where:** `web/src/lib/{format,chart,indexer}.test.ts` only. `vitest` environment node. 105 tests green without App render.

`npm test` stays green while BUG-G1 and BUG-G3 are red.

---

## Wiring status (not a product bug)

- `gno.land/p/bazaar/grc721/` copied 12:53 (untracked).
- `nft.gno` 13:00:15: `attachToken`, `grcMint`, `grcTransfer`, `collection.token/ledger`, `tokenSeq` / `symbols`. `nft_test.gno` `resetAll` clears those trees.
- Wiring **not** finished for `gno test` because GRC721 does not compile (BUG-G1).
- Pearl still nftv5 book (no GRC721 `/p/bazaar`).

---

## Leftover (not re-dumped this pass)

- BUG-L1 BottomNav overlap @ ≤1080px (`docs/qa/2026-09-20-launch-ui.md`).
- Indexer `/api` 500 when `8788` is down (console on collection pages).
