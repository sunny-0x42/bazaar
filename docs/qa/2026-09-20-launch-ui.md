# QA Launch UI polish — 2026-09-20

Owner: bazaar-qa (Dew). Vitest + `tsc` + `gno test` + Puppeteer dump Vite `127.0.0.1:5176`. Không code fix. Không deploy Pearl.

Screenshots: `web/tmp-qa/launch-polish/`.

---

## Commands

| Command | Cwd | Exit | Result |
| --- | --- | --- | --- |
| `gno test ./gno.land/p/bazaar/fee/v1/` | repo | 0 | `ok … 0.73s` |
| `gno test ./gno.land/r/bazaar/` | repo | 0 | `ok … 0.73s` |
| `gno test ./gno.land/r/bazaar/market/` | repo | 0 | `ok … 0.86s` |
| `gno test ./gno.land/r/bazaar/nft/` | repo | 0 | `ok … 1.33s` |
| `gno test ./gno.land/p/bazaar/dropstd/v1/` | repo | 0 | `ok … 0.90s` |
| `npx tsc --noEmit` | `web/` | 0 | stdout trống |
| `npm test -- --run` | `web/` | 0 | 3 files, **103 tests** PASS |

`gno test ./gno.land/p/bazaar/dropstd/` (không `/v1`) FAIL setup: `gnomod.toml doesn't exist`. Không phải regress Launch UI.

Pearl read-only `vm/qeval` `nftv5.LaunchFee()` → `(1000000000 int64)` = **1000 GNOT**. `ListOpen()` live.

---

## Route PASS/FAIL

| Route | Viewport | Verdict | Notes |
| --- | --- | --- | --- |
| `#/explore` Browse | 1280 / 720 | **PASS** | 12 collections, LIVE ticker, Install Adena. 720: BottomNav che featured card (leftover). |
| `#/launch` Studio | 1280 / 720 | **PASS** | Wizard 1–5. `launch-shell` `max-width: none` (1232px / 700px). Không `form-narrow`. Grid 2 cột → 1 cột @720. |
| `#/launch` Load items | 1280 / 720 | **PASS** | Empty: “Create a collection in Studio first”. Download JSON template. Không mojibake. |
| `#/launch` Mint 1/1 | 1280 / 720 | **PASS** | Item name input **44px** = Collection `.select-btn` **44px**, radius 10px, `rgb(12,15,21)`. Connect Adena. |
| `#/m/stones` | 1280 / 720 | **PASS** | Signal Stones, 0.5 GNOT, 3/6, Connect Adena to mint, Launch + Collection. |
| `#/c/stones` | 1280 | **PASS** | Items / Buy / Offer / Mint · 0.5 GNOT. |
| `#/sell` | 1280 | **PASS** | Empty Connect Adena (chưa wallet). |
| `#/profile` Mine | 1280 | **PASS** | Empty Connect Adena. |
| `#/settings` | 1280 | **PASS** | Pearl, nftv5 path, Connect Adena to seed. |
| Connect | all | **PASS** header | Không Adena → `Install Adena` → `https://adena.app/`. |
| wrong-network | — | **PASS source** | Không Adena session. `Notices`: “Adena is on {chainId}. Switch to {wantChainId}…”. `call()` throw trong try. |
| empty list | sell/profile/load | **PASS** | Sell/Profile empty-state. Load items text-only. Explore không empty (catalog + chain). |

---

## Polish checklist (parent)

| Item | Result |
| --- | --- |
| Wider `launch-shell`, no `form-narrow` 480/560 | **PASS** computed `maxWidth: none` |
| Inputs/selects 44px `--target`, same radius, `var(--input)` | **PASS** Launch form. Header search vẫn 40px @1280 (ngoài scope). |
| Mojibake Launch.tsx `1-5`, `open`, `/` | **PASS** “Follow steps 1-5”. DOM `mojiHits: []` (không `â€` / `Â·`). |
| Wizard two-column `launch-grid` | **PASS** `589px 589px` @1280; `1fr` @720 |
| Create fee 1000 GNOT copy | **PASS** sau refresh. Banner luôn “default 1000 GNOT”. Step 5 sau 8s: “Create fee is 1000 GNOT”. |

---

## Leftover bugs

### BUG-L1 — P2 BottomNav che field / card @ ≤1080px

**Where:** `web/src/styles.css` `@media (max-width: 1080px)` `.bottom-nav { position: fixed }`; `main { padding-bottom: 16px }` (720). Chỉ `.site-footer { margin-bottom: 96px }`.

**Repro**

1. `cd web; npm run dev` → http://127.0.0.1:5176
2. Viewport 720×900.
3. `#/launch` Studio step 1 — Description / Website nằm dưới BottomNav.
4. `#/launch` Studio step 3 — “Mint rounds” / Public mint only bị nav đè.
5. `#/explore` — featured card thứ 3 bị nav đè.

**Expected:** nội dung form/card không nằm dưới nav (~61px).  
**Actual:** `main` pad 16px. Dump `launch-studio-720.png`, `launch-wizard-step3-720.png`, `explore-720.png`.

Không overlap input-vs-input (Puppeteer `overlaps: []`).

### BUG-L2 — P3 Wizard Create fee “Free” trước khi `LaunchFee()` về

**Where:** `App.tsx` `useState(0)` + `LaunchWizard` `launchFeeUgnot > 0 ? … : "free"`. `call(..., launchFeeUgnot > 0 ? \`${launchFeeUgnot}ugnot\` : "")`.

**Repro**

1. Hard refresh `#/launch`, đi 1→5 trong ~2s (trước khi `refresh()` xong).
2. Step 5: “Create fee is free” / facts **Free**. Banner đã ghi default 1000 GNOT.
3. Đợi ~8s, reload walk: facts **1000 GNOT**.

Pearl `LaunchFee()` = 1000000000. Nếu user Launch khi state còn 0 → `send=""` → realm `takeLaunchFee` panic `nft: launch fee`.

### BUG-L3 — P3 In-page “Connect Adena” khi header là “Install Adena”

**Where:** `Header.tsx` `hasAdena()` → Install vs Connect. `Launch.tsx` Mint 1/1, `LaunchWizard` “Connect Adena to launch”, `MintPage`, `Sell` luôn `onConnect`.

**Repro:** Chrome không Adena. Header = Install Adena (link). Mint 1/1 / Sell / `#/m/stones` = Connect Adena. Click → toast `Install Adena at https://adena.app/` (`assertAdena`).

### BUG-L4 — P3 Load items empty không có nút Studio

**Where:** `Launch.tsx` pane `slots` khi không drop: chỉ `<p className="muted">Create a collection in Studio first…</p>`. Có Download JSON template, không “Studio” / “Create collection”.

---

## Hunt notes

- Unicode còn trong wizard (`—` `–` `·` `≥` `…` `∞`) là UTF-8 thật, **không** mojibake trên page (`charset=UTF-8`).
- `Connect` / empty / wrong-network: source giữ. Adena live / switch chain: **chưa** (headless không wallet).
- Console: 404 + `NotSameOrigin` + RPC 500 trên sample/indexer — không crash page (`pageErrs: []`).
