# QA rarity — 2026-09-19

Owner: bazaar-qa (Dew). Source + `gno test` + vitest + `tsc` + gnomcp Pearl. Không browser/Adena live. Không code fix. Không deploy.

Đối chiếu `ItemLine`, `ListingDrawer` (item-detail), `ListingCard`, `catalog.json`. Pearl addpkg **bị chặn** (read-only).

---

## Commands

| Command | Cwd | Exit | Result |
| --- | --- | --- | --- |
| `gno test ./gno.land/r/bazaar/nft/` | repo | 0 | `ok      .\gno.land\r\bazaar\nft 1.00s` |
| `cd web; npm test` | `web/` | 0 | 3 files, **79** tests PASS (`format.test.ts` 55, `chart.test.ts` 16, `indexer.test.ts` 8) |
| `cd web; npx tsc --noEmit` | `web/` | 0 | stdout trống |

Không failure realm/web để quote. `npm test` / `tsc` từ **repo root** vẫn ENOENT / fake `tsc` — phải `cd web`.

Realm `TestMintListBuy` / `TestSeedSamples` assert `ItemLine` **có** field rarity (xem Confirm). Vitest `parseItems` đọc `parts[8]`. **Không** vitest render `ListingCard` / `ListingDrawer`.

---

## Confirm (pass)

### `ItemLine` có rarity

`gno.land/r/bazaar/nft/nft.gno`:

```go
func ItemLine(id int) string {
	it := mustItem(id)
	return strconv.Itoa(it.id) + "|" + it.name + "|" + it.owner.String() + "|" + it.seller.String() + "|" + strconv.FormatInt(it.price, 10) + "|" + strconv.FormatBool(it.listed) + "|" + it.image + "|" + it.collection + "|" + it.rarity
}
```

9 field: `id|name|owner|seller|price|listed|image|collection|rarity`.

`RarityOf(id)` đọc `it.rarity`. `mintTo` set `rarity: rarityOf(id)` — deterministic từ `id % 10` (không RNG). `SeedSamples` override bằng `setRarity` khớp catalog.

Test quote:

```
want := "1|Sword|" + bob.String() + "||0|false|https://example.com/sword.png|bazaar|Common"
want := "1|Ember Core|" + self.String() + "|" + alice.String() + "|1000000|true|/samples/stones-01.png|stones|Legendary"
```

Web: `parseItemLine` → `rarity: (parts[8] || "").trim()`. Vitest row `stones|Legendary` PASS.

### `ListingDrawer` là item-detail + rarity

`web/src/components/ListingDrawer.tsx`:

- `aside className="item-detail"` `role="dialog"` `aria-labelledby="drawer-title"`
- `rarity = item.rarity || "Common"`
- badge `<span className={`rarity rarity-${rarity.toLowerCase()}`}>{rarity}</span>`
- `<dt>Rarity</dt><dd>{rarity}</dd>`

CSS: `.item-detail`, `.rarity`, `.rarity-common` … `.rarity-legendary` (`styles.css` 481–495).

### `ListingCard` hiện rarity

```tsx
{item.rarity ? <div className="listing-id"><span className={`rarity rarity-${item.rarity.toLowerCase()}`}>{item.rarity}</span></div> : null}
```

Empty string → không badge (drawer vẫn fallback `"Common"`). Catalog preview luôn `it.rarity || "Common"` nên sample card có badge.

### `catalog.json` có rarity

`web/public/samples/catalog.json` — 9/9 items:

| Collection | Item | rarity |
| --- | --- | --- |
| stones | Ember Core / Tide Core / Night Core | Legendary / Uncommon / Rare |
| lamps | Copper Wick / Blue Wick / Paper Wick | Common / Rare / Epic |
| relics | Folded Sun / Folded Tide / Folded Gate | Uncommon / Rare / Legendary |

`parseCatalog` đọc `rec.rarity`. `previewItemsFor` → `rarity: it.rarity || "Common"`. `SeedSamples` cùng map (realm test ItemLine Ember Core = Legendary).

### Pearl **không** addpkg được qua gnomcp (read-only)

Không broadcast. Probe: `gno_addpkg` `profile=pearl` `simulate=true` `deploy_path=gno.land/r/bazaar/nft`.

Quote reject:

```
gno_addpkg: signer: keystore: chain-id "pearl-1" is read-only — read-only chains have no agent key
```

`gno_profile_list`:

- `pearl` — chain `pearl-1` · **read-only (mainnet/betanet)** · rpc `https://rpc.pearl.testnets.gno.land:443`
- `test16` — cùng `pearl-1` · **read-only**
- `gno_addpkg` schema enum writable: `local` / `test13` / `testnet` — **không** có `pearl` / `test16`

`gno_status` pearl: node up, height **537144**, chain-id `pearl-1` (2026-09-19T06:11:15Z).

`gno_packages` `gno.land/r/bazaar` profile `pearl` → **rỗng** (chưa addpkg). `gno.land/p/demo/tokens` → chỉ `grc20`.

AGENTS.md: “Pearl addpkg needs an explicit human yes. gnomcp `pearl` is read-only.” Tool đã fail-closed trước CLA/gas.

---

## Surfaces (Browse / Sell / Create / Mine / Connect / wrong-network / empty list)

Source review. Không Adena session. Tab id: Explore / Sell / Launch (`create`) / Portfolio (`portfolio`).

| Surface | File | Kết quả |
| --- | --- | --- |
| Browse / Explore | `Explore.tsx` | For sale + Collections. Pearl missing → `Realm not on Pearl yet. Showing sample collections.` + Open Settings. Sample For sale vẫn hiện (BUG-BUY-1 còn mở). Card rarity từ catalog. |
| Sell | `Sell.tsx` | Disconnect → Connect Adena. Connected empty → `No unlisted items` + Launch. List `disabled={blocked \|\| busy \|\| !canList}`. |
| Create / Launch | `Launch.tsx` | Drops + Create drop. Create `disabled={blocked \|\| busy \|\| !canCreate}`. Preview Mint khi connected vẫn `PublicMint` (BUG-BUY-3). |
| Mine / Portfolio | `Portfolio.tsx` | Disconnect vs `No items yet` tách copy. Listed → Cancel (`blocked \|\| busy`). Unlisted → Sell. **Không** hiện rarity. |
| Connect | `Header.tsx` | `window.adena` → Connect Adena. Không → `<a href="https://adena.app/">Install Adena</a>` (không `onConnect`). Thiếu `chainId` → throw (không default `pearl-1`). |
| wrong-network | `Notices` + `call()` | Banner `Adena is on {id}. Switch to {wantChainId} to buy or sell.` Guards trong `try` → `.err`. Drawer Buy disable. **Card Buy không disable** (BUG-BUY-2 còn mở). |
| empty list | Explore / Sell / Portfolio | Sell / Portfolio có empty. Explore `ListOpen()` rỗng + catalog load → sample For sale, **không** `No open listings`. |

UI English. Network Local / Pearl. BottomNav ≤720px: Explore / Launch / Sell / Portfolio.

---

## BUG-RARITY-1 — docs `ItemLine` thiếu field rarity

**Severity:** P3 (docs vs code; parser UI đã 9 field)  
**Where:** `docs/NFT.md` bảng API; `docs/SAMPLES.md` dòng ItemLine.

Code + test:

`id|name|owner|seller|price|listed|image|collection|rarity`

Stale:

- `docs/NFT.md`: `id\|name\|owner\|seller\|price\|listed\|image\|collection` — không `RarityOf`
- `docs/SAMPLES.md`: `ItemLine: id|name|owner|seller|price|listed|image|collection`

**Repro**

1. Đọc `nft.gno` `ItemLine` / `TestSeedSamples` want `…|stones|Legendary`.
2. Đọc `docs/NFT.md` hàng `ItemLine`.

**Expected:** 9 field + `RarityOf`.  
**Actual:** 8 field. Client làm theo docs sẽ drop rarity (`parts[8]` empty → card không badge).

---

## Bugs còn mở (không regression rarity)

Từ `docs/qa/2026-09-19-buy.md` — source **chưa** đổi:

- **BUG-BUY-1** P2 — `ListOpen()` rỗng → For sale = catalog preview + Buy.
- **BUG-BUY-2** P2 — card Buy không `blocked` / `busy` / self-buy.
- **BUG-BUY-3** P3 — preview listed đã Connect → `onConnect()` lại, không copy sample.

Coverage: `npm test` 79 PASS **không** render card/drawer. `parseCatalog` fixture trong `format.test.ts` **không** gắn `rarity` (default `""`); catalog.json live thì có.

---

## Không làm

- Không deploy / addpkg Pearl (probe `simulate=true` bị reject ở keystore).
- Không implement fix.
- Không xóa test.

## Invariants giữ

- `ItemLine` 9 field; `SeedSamples` rarity = catalog.
- `rarityOf` deterministic (`id % 10`), không homemade RNG.
- Buyer send = `row.price` ugnot. Protocol 50 bps từ price.
- `call()` money-path trong `try/catch` → `.err`.
- Pearl gnomcp **read-only**: không agent key, không addpkg.

## Assumptions chưa chứng

- Browser: badge `.rarity-legendary` trên Ember Core (source + CSS có; không screenshot).
- Adena `GetAccount().data.chainId` sau `SwitchNetwork` fail.
- `gnodev` Init + SeedSamples: For sale = 9 live rows với rarity, không catalog.
- `filterItems` / Explore search **không** match rarity — search `"Legendary"` có thể miss.
