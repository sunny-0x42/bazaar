# QA UI — 2026-09-19

Owner: bazaar-qa (Dew). Source review + vitest + `tsc` + Pearl `abci_query`. Không browser/Adena session. Không code fix. Không deploy.

Đối chiếu `docs/copy/ui-en.md`, `docs/research/01-marketplace-ux.md`, và QA realm cùng ngày (`docs/qa/2026-09-19.md`).

---

## Commands

| Command | Cwd | Exit | Result |
| --- | --- | --- | --- |
| `gno test ./gno.land/p/bazaar/fee/v1/` | repo | 0 | `ok … 0.70s` |
| `gno test ./gno.land/r/bazaar/` | repo | 0 | `ok … 0.73s` |
| `gno test ./gno.land/r/bazaar/market/` | repo | 0 | `ok … 0.86s` |
| `cd web; npm test` | `web/` | 0 | `src/lib/format.test.ts` **14 tests** PASS (365ms) |
| `cd web; npx tsc --noEmit` | `web/` | 0 | stdout trống |

`npm test` / `npx tsc --noEmit` từ **repo root** fail (`ENOENT package.json` / `tsc` không phải compiler). Phải `cd web`.

Vitest vẫn `environment: node`. Không render `App` / tab / drawer. Coverage surface QA = source + helper unit tests.

---

## Confirm (pass)

### `call()` try/catch

`web/src/App.tsx` `call()` (khoảng 120–141): `setBusy` rồi **`try` chứa cả ba guard** (`!account`, `wrongNet`, `!isPkgPath(market)`) + `doContractCall`. `catch` → `setErr`. `finally` → `setBusy("")`. Click handlers `void call(...)`.

BUG-1 buổi sáng (guard **trước** `try`, unhandled rejection) **đã đóng trên source**.

### Pearl banner

`TestnetRibbon`: `Pearl testnet — test GNOT has no market value.` Khớp `banner.testnet` trong `docs/copy/ui-en.md`. Luôn mount trên `App`. Header thêm badge `Pearl`.

### English + không fake USD

- `index.html` `lang="en"`. Title/description English.
- Không chuỗi tiếng Việt trong `web/src`.
- Không `USD` / `$` / fiat trong `web/src` (chỉ GNOT / ugnot).
- `gnotFromUgnot` → `{n} GNOT`. Card / drawer / Sell / Portfolio cùng đơn vị.

### 0.50% fee

`PROTOCOL_BPS = 50`. `protocolFeeLabel()` → `"0.50%"`. Vitest: `protocolFeeUgnot(1_000_000, 50) === 5000`, label `"0.50%"`.

| Surface | Copy | Math |
| --- | --- | --- |
| Explore `StatsRow` | Protocol fee **0.50%** | label only |
| Sell aside | Buyer pays / Protocol (**0.50%**) / You receive | `fee = protocolFeeUgnot(priceUgnot)`, `receive = price − fee` |
| Listing drawer | Protocol fee **0.50%** + You pay = **listing.price** | Buyer send = `row.price` ugnot (khớp realm `Buy`: `sent != row.price` → panic) |

Realm: fee lấy **từ** `price`, seller nhận `price − proto`. Không cộng thêm lên buyer.

### Connect Adena / Install Adena / wrong-network

- Có `window.adena` → `Connect Adena`. Không có → `<a href="https://adena.app/">Install Adena</a>` (không gọi `onConnect`).
- `connectAdena`: thiếu `chainId` → throw `Adena did not report a chain. Switch to Pearl (pearl-1).` (BUG-2 default `pearl-1` **đã đóng**).
- `wrongNet` = `account.chainId !== PEARL.chainId`. Banner `Notices`. `blocked` disable List / Create / Buy / Cancel. `call()` vẫn throw trong `try` → `.err`.

### Empty vs realm-missing (`isRealmUnavailable`)

`refresh` không nuốt lỗi `ListOpen()` thành empty book.

- Fail `evalExpr` → `chainNote = Could not read listings: ${message}`, `listings = []`.
- `isRealmUnavailable` nếu message khớp `not on this chain` / `package not found` / `unknown package` / `invalid package`.
- Explore: `missing` → title **Realm not on Pearl yet** + body deploy/gnodev + **Open Settings**. Không `missing` → **Could not read the order book**.
- Không `chainNote` + `listings.length === 0` → **No open listings** (empty book thật).

Pearl live (read-only, 2026-09-19): `gno_packages` `gno.land/r/bazaar` profile `pearl` = rỗng. `abci_query` `vm/qeval` `gno.land/r/bazaar/market.ListOpen()`:

```
Error: /vm.InvalidPkgPathError
Log: … package not found: gno.land/r/bazaar/market
```

`abciErrorMessage` → `Realm not on this chain yet: gno.land/r/bazaar/market`. Vitest `isRealmUnavailable` phủ prefix `Could not read listings: …`. BUG-3 buổi sáng **đã đóng trên source + RPC**.

Hub: `refresh` gọi `GetModule("market")` khi hub path hợp lệ; fail thì giữ market đã paste. BUG-4 **đã đóng trên source**.

### Tabs (source)

| Surface | File | CTA / empty | Ghi chú |
| --- | --- | --- | --- |
| Explore | `Explore.tsx` | Card → drawer Buy; empty Create/List | Search/sort; skeleton khi loading |
| Sell | `Sell.tsx` | List / Connect Adena | Fee math GNOT; **không** `sell.empty` |
| Create | `Create.tsx` | Create / Connect Adena | Hint 2–8 A–Z0–9, decimals 0–6; CTA không disable khi pattern sai |
| Portfolio | `Portfolio.tsx` | Cancel | Disconnected vs connected-empty tách copy |
| Settings | `Settings.tsx` | Hub / Market / RPC / Faucet / gnoweb | Gear header; **không** nằm BottomNav |
| Listing drawer Buy | `ListingDrawer.tsx` | Buy / Connect Adena; Escape đóng | `blocked \|\| busy` disable Buy |
| BottomNav | `BottomNav.tsx` | Explore, Sell, Create, Portfolio | `display:none` desktop; `display:grid` **≤720px** |

---

## BUG-UI-1 — Tab nav biến mất ở viewport 721–900px

**Severity:** P2  
**Where:** `web/src/styles.css`

```css
@media (max-width: 900px) {
  .desk-nav { display: none; }
}
@media (max-width: 720px) {
  .bottom-nav { display: grid; }
  main { padding-bottom: 112px; }
}
```

`.bottom-nav` default `display: none`. Khoảng **721–900px**: không `desk-nav`, không `BottomNav`.

**Repro**

1. `cd web; npm run dev` → http://127.0.0.1:5176
2. DevTools width **800px** (không phải 720).
3. Tìm Explore / Sell / Create / Portfolio.

**Expected:** một trong hai nav (desktop tabs hoặc BottomNav).  
**Actual:** chỉ logo (→ Explore), search (→ Explore), badge Pearl, Connect, gear Settings. **Sell / Create / Portfolio không tới được.**

≤720px BottomNav hiện; Settings vẫn qua gear (cố ý).

---

## BUG-UI-2 — Listing drawer fee dễ đọc thành cộng thêm

**Severity:** P2 (copy money-path; send amount vẫn đúng)  
**Where:** `web/src/components/ListingDrawer.tsx`  
**Spec:** `docs/research/01-marketplace-ux.md` §3 — Protocol fee (0.50%) = **included in the price**; hiện Seller receives + You receive `{amount} {symbol}`.

Drawer hiện:

- Price = `{price} GNOT`
- Protocol fee 0.50% = `{fee} GNOT`
- You pay = `{price} GNOT`

Không chữ **included**. Không dòng seller net. Không dòng token nhận.

**Repro**

1. Explore có listing (local `gnodev`; Pearl chưa addpkg).
2. Click card → drawer.
3. Đọc You pay vs Protocol fee.

**Expected:** You pay = listing price; fee **included**; seller net = price − fee.  
**Actual:** hai dòng số cạnh nhau, fee trông như extra. Tx vẫn `send = ${row.price}ugnot` — Adena không cộng fee.

Sell aside **đúng** (Buyer pays / Protocol / You receive).

---

## BUG-UI-3 — Copy lệch `docs/copy/ui-en.md`

**Severity:** P3  
UI English, không investment language. Lệch source-of-truth:

| Key | ui-en.md | Code |
| --- | --- | --- |
| `hero.title` / `hero.lead` | Có | **Không** hero (StatsRow thay) |
| `explore.empty` | No open listings. Create a token, then list it. | No open listings / Be the first to mint a GRC20… |
| `wallet.wrongNetwork.named` | Adena is on {chainId}. Switch to Pearl (`pearl-1`). | Adena is on {chainId}. Switch to {chainId} to send transactions. |
| `sell.title` / `sell.lead` | List tokens / Listing **moves** tokens… | **Sell** / Listing **pulls** tokens… |
| `sell.empty` | Nothing to list yet. Create a token first. | **Không** empty state (form luôn mở) |
| `create.title` / `create.lead` | Create a GRC20 / …unique, 2–8 letters or digits. | Create token / …unique on this market. |
| `btn.create` connected | Create | Create (OK) — heading vẫn “Create token” |
| `portfolio.empty.*` | Connect Adena to see your listings. / You have no open listings. | Connect a wallet to see listings you opened… / You have nothing in escrow… |
| `fee.line` | 0.50% protocol fee | Protocol (0.50%) / Protocol fee 0.50% |
| `banner.testnet` | Pearl testnet — test GNOT has no market value. | **Khớp** |
| `wallet.connect` / `wallet.install` | Connect Adena / Install Adena | **Khớp** |

`TestnetRibbon` không link faucet (research: banner + faucet). Faucet chỉ Settings.

---

## BUG-UI-4 — Thứ tự tab mobile ≠ desktop / ui-en

**Severity:** P3  
Header `DESK_NAV`: Explore, **Create**, Sell, Portfolio (`ui-en` Nav).  
`BottomNav` `ITEMS`: Explore, **Sell**, Create, Portfolio.

**Repro:** width ≤720px vs desktop. Cùng bốn tab, Create/Sell đảo chỗ.

---

## BUG-UI-5 — Không vitest cho tab / Connect / drawer

**Severity:** P2 (coverage)  
`format.test.ts` phủ fee, parse, filter/sort, `isRealmUnavailable`. Không test:

- Explore empty vs realm-missing vs no-matches
- Sell fee preview 0.50%
- Create / Portfolio empty
- Connect Adena vs Install Adena
- wrong-network banner + `blocked`
- ListingDrawer Buy disabled
- BottomNav 4 item + Settings gear

`npm test` / `tsc` green **không** chứng minh BUG-UI-1/2.

---

## Checklist

| Check | Kết quả |
| --- | --- |
| Explore | Pass source; Pearl → realm-missing (không empty book) |
| Sell | Pass; fee 0.50% GNOT; thiếu `sell.empty` |
| Create | Pass; English; pattern hint only |
| Portfolio | Pass; empty disconnected ≠ connected |
| Settings | Pass; paths + RPC + faucet |
| Connect Adena | Pass; Install = link adena.app |
| wrong-network | Pass source (banner + disable + `call` catch) — chưa Adena live |
| empty vs realm-missing | Pass source + Pearl RPC + unit test |
| Listing drawer Buy | Pass wiring; copy fee = BUG-UI-2 |
| mobile BottomNav | Pass ≤720px; **fail 721–900px** (BUG-UI-1) |
| English | Pass |
| No fake USD | Pass |
| Pearl banner | Pass (khớp ui-en) |
| 0.50% Sell + drawer | Pass số; drawer wording = BUG-UI-2 |
| `call()` try/catch | Pass |

---

## Không làm trong pass này

- Không deploy / addpkg Pearl.
- Không implement fix.
- Không xóa test để green.
- Không browser/Adena (wrong-network live, SwitchNetwork ignore vẫn residual — GetAccount không còn default `pearl-1`).

## Invariants giữ (UI)

- Buyer send = listing `price` ugnot; protocol 50 bps lấy từ price.
- Money-path UI error vào `.err` qua `try/catch`.
- Empty book ≠ package missing.

## Assumptions chưa chứng

- Adena `GetAccount().data.chainId` đúng network sau `SwitchNetwork` fail.
- `toLocaleString(undefined)` trên máy vi-VN chỉ đổi nhóm số, không chèn USD (không gọi `style: currency`).
- Race `GetModule` `setMarket` vs `call()` dùng `market` state — không tap trong lúc `refresh` lần đầu.
